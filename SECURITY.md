# Security

Never commit these secrets to GitHub:

- Discord bot token
- MongoDB connection string

Keep them only in Render environment variables.

If either secret is exposed, rotate it immediately.
